const hewan = [
    {nama: 'anjing', umur: 24},
    {nama: 'kucing', umur: 23},
    {nama: 'cacing', umur: 21},
];

for(let i = 0; i < hewan.length; i++ ){

    console.log(`nama saya adalah ${hewan[i].nama}`);
    console.log(`usi saya adalah ${hewan[i].umur}`);
}